Use case: ui-mockup
Asset type: one high-fidelity desktop application UI concept screenshot, 1600x900 landscape
Primary request: Create an edge-to-edge native desktop screenshot concept for cw-code named “Session Ribbon,” a T3-Code-style desktop shell that manages many real Claude, OpenCode, and Codex CLI sessions.
Scene/backdrop: dark neutral professional developer-tool interface, Rider-like clarity, edge to edge with no device frame.
Composition/framing: exactly 1600x900 landscape. At the far left, a very slim vertical project rail with compact project glyphs and tiny-but-readable project names such as “Payments API” and “Sessions.” Across the full top, clearly distinct from the rail, a horizontal grouped session ribbon with concurrent session tabs from several projects. It must visibly include labels “Payments API”, “Auth refactor”, “Sessions”, “Refactor realtime auth”, with one active session. Beneath this top ribbon, leave one large uninterrupted, spacious active conversation canvas—conversation is unmistakably primary, not a dashboard. In the lower right, attach a compact floating/dockable tool tray shelf, visibly detachable/drag-ready via a small grip, with four mode labels: “Files”, “Diff”, “Agents”, “Terminal”. It must not be a permanent right sidebar.
UI content: sparse useful chat: title “cw-code”; active turn “Refactor realtime auth”; small “Running” status; one compact tool activity summary “3 files changed”; a substantial bottom composer with placeholder “Ask cw-code”. Use simple small icons and restrained code/tool summaries, no long prose.
Style/medium: high fidelity real native app screenshot, dark graphite and near-black neutral surfaces, sans UI typography at deliberately larger 110–115% density/scale, excellent hierarchy. Selective subdued blue/cyan for active controls and coral for one alert/status accent. Crisp typography, natural shadows only where needed.
Color palette: dark neutral graphite surfaces; restrained blue/cyan active accents; single coral accent.
Constraints: visual structure must be clearly different from an app with a fixed right panel. Use whitespace, large surface fields, and positional grouping for separation. The session ribbon must be visibly multi-project and concurrent. Keep all requested labels legible and correctly spelled.
Avoid: card grids, dashboard tiles, permanent right-column editor, excessive borders, divider-line-heavy layout, rounded outlined cards everywhere, gradients, neon glow, glassmorphism, tiny illegible text, verbose filler copy, marketing poster styling, laptop/device frame, watermark.
