# Base generation prompt (used to produce the edit target)

Use case: ui-mockup
Asset type: final 1600x900 desktop application screenshot.
Create a dark high-fidelity native desktop UI for cw-code titled conceptually “Live Project Board.” This must visually be a HORIZONTAL project-session BOARD, decisively not a left-sidebar / center-chat / right-inspector layout and not three equal vertical columns. Compose it as an asymmetrical editorial Swiss grid with four wide project zones horizontally across the top half, separated mostly by empty space and changes in surface plane rather than borders. Project headings: Payments API, Desktop Shell, Docs. Under and around each heading, sessions are flat rectangular color fields of varied sizes. One selected “visual redesign” field breaks out and grows organically across the central-right two zones into a broad thread focus canvas; retain nearby small tiles as context at its left/top/bottom. Place auth refresh and tests as small supporting tiles. Status labels only: Running and Waiting. This should feel like a landscape command board for active parallel coding work.
At the top, a very thin global dark app bar with only cw-code and Projects. At the bottom, a compact flush command shelf: Thread, Files, Diff, Agents, Terminal, then minimal input text Ask cw-code. No permanent inspector.
Use dark graphite and almost-black flat planes, off-white Swiss type, one cobalt blue active field, a small acid-lime status accent, one amber field, muted grey Waiting fields. Large 110–115% scale, few huge clear labels, high contrast. Very low border count: no outlines around every item, no card-grid styling, no rounded glass UI. No gradients, no glow, no decorative charts, no sidebar, no device frame, no browser chrome, no verbose sentences, no tiny metrics. Preserve labels accurately and minimize all other text.

# Final edit prompt (applied to the base generation; this produced the final image)

Use case: precise-object-edit
Asset type: final desktop UI screenshot.
Input image role: edit target. Preserve the entire asymmetrical horizontal Swiss project board composition, dark flat surfaces, typography scale, lane headings, tile positions, bottom shelf, and all existing readable labels exactly. Edit ONLY the large cobalt-blue selected "visual redesign" field: make it visibly a live expanded conversation focus, not merely a blank tile. Keep its huge "visual redesign" title and its Running label. Add a very sparse, crisp, minimal thread structure in the lower half of the blue field: two short small white/near-white conversation lines or compact code/tool-status lines, such as “cw-code” and “tests ready”, with a tiny lime active status dot, leaving large empty blue space. Do not add verbose prose. Keep it flat, no chat bubbles, no new borders, no gradients, no glow. Exact visual integrity matters.
