Refine this exact cw-code Rider Violet Workbench screenshot. Preserve its three-region structure, panel widths, project/session hierarchy, right Files + diff + terminal workspace, realistic code content, icon-first top toolbar, typography, and violet/graphite identity.

Make the center conversation canvas visibly darker and calmer than the sidebar and tooling pane, roughly one tonal step toward near-black navy graphite. Reduce violet/accent use inside the thread; reserve accents for active status, code diff, and primary controls. Messages must read as coding work records, not chat: remove every circular avatar/icon beside both user and assistant messages and remove all resting timestamps. Keep only small typographic speaker labels such as “You” and “Claude” if needed for orientation. Timestamp and copy are hover-only and should not appear in this resting-state comp. Use spacing and typography to distinguish turns, with no bubble outlines or separators.

Slim the bottom Command Deck significantly. The writing plane should be about 76–84px tall with restrained 12px padding, blending into the dark thread without an outline. Beneath it, make the utility rail only about 34–38px tall with 6–8px vertical padding. Controls should sit as lightweight icon-plus-short-value clusters separated by generous whitespace only—no vertical rules, no horizontal line, no boxed pills, no segmented-control blocks. Reduce the visual weight of provider, effort, permissions, context, and branch. Keep a 40–42px violet rounded-square send key detached from the rail by a clear 10px gap. The composer should feel precise and light, not like a padded block.

Remove any remaining routine row separators. Use surface tone, rhythm, and alignment for structure. No new labels, panels, cards, activity rails, or marketing elements. Production desktop IDE screenshot, same 1920×1080-style landscape composition.
