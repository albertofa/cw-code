Make the same precise composer and git-context refinement to this exact cw-code Rider Violet Agents screenshot. Preserve the complete three-region structure, label-free user/agent work log, compact agent roster above the full-width selected-agent inspector, all statuses, event ledger, palette, and content.

Split the composer into two clearly separate pieces. The upper writing box remains substantial and full width at about 96–104px tall. Directly beneath it, after a visible 7–9px gap, place a smaller detached tooling shelf only about 62–70% of the writing box width and 38–42px tall, aligned left. Its mid-graphite tone must be clearly visible against the dark canvas. The shelf contains only attachment, Claude Sonnet provider/model, High effort, Auto permissions, and 3 files/context. No separator lines, pills, or boxes. Remove git branch/worktree from the composer entirely. Keep the violet 42px send/stop key detached at far right on the shelf baseline, with empty space between.

Move session git context to the center conversation header beside “Visual design pass” as a muted branch icon plus “chore/visual-design-pass”, with no pill or border. Remove the duplicate branch label from the far-right global title bar. Preserve every other element exactly, especially the improved vertical Agents layout and message alignment.
