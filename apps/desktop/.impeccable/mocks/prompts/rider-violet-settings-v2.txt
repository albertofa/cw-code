Transform this cw-code Settings screenshot into a large focused settings window layered above the existing workbench. The background workbench should be visible only as a very dim, slightly softened inert backdrop around the edges. Do not include the project/session sidebar inside the Settings window itself.

The centered settings window should occupy roughly 82–86% of the screen width and 84–88% of the height, with restrained Rider-style graphite depth and modest corner rounding. Its own title bar contains a back/close icon, “Settings”, subtle unsaved-state dot if needed, and the single Save action. Inside, use exactly three working regions: a compact settings-category rail (General, Appearance, Providers, Source Control, Worktrees, Permissions), a narrow provider roster (Claude Code, OpenCode, Codex with availability dots and versions), and the selected Claude Code detail filling the remaining width. No application/project sidebar as a fourth column.

Preserve the real fields from the reference: binary path with icon-only browse/recheck controls, launch arguments, enabled model list with default star and switches, custom model, CLI availability/version/path. Keep the active Providers marker violet. Reduce helper copy and visual noise. No routine horizontal separators, no row borders, no card grid. Use broad tonal regions, spacing, aligned controls, and one focused input outline. Bottom or title-bar actions must be minimal: icon-only cancel/close/reset where present, with Save as the only labeled primary action.

Maintain the exact Rider Violet palette, larger readable scale, Segoe-like UI type, mono only for paths/versions. No marketing elements, no fake settings, no glassmorphism.
