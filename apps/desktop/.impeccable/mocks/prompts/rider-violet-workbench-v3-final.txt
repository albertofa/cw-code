Make one precise message-layout correction to this exact cw-code workbench screenshot. Keep all speaker names, avatars, and timestamps absent. Move only the first user request, “Polish the UI with better spacing and hierarchy, but keep the same three-pane layout.” into a compact content-sized neutral graphite block aligned to the right side of the reading column, like T3 Code. Maximum width about 72%, 12–14px padding, 18px soft corners, no border, no accent color. Keep the assistant response and task result rows left-aligned and open directly on the canvas with no bubble. Preserve the composer exactly as shown, including its clearly elevated writing plane, visible darker tooling shelf, no separators, and violet send key. Preserve every other pixel-level structure and detail.
