Make a precise composer and git-context refinement to this exact cw-code Rider Violet workbench screenshot. Preserve the complete three-region layout, label-free T3-style work log, right file/diff/terminal pane, sidebar, palette, scale, and all content.

Split the composer into two clearly separate pieces. Keep the upper writing box at its current substantial width and about 96–104px tall, with the same softly elevated graphite/plum surface and placeholder. Directly beneath it, leave a visible 7–9px gap, then place a smaller detached tooling shelf that is only about 62–70% of the writing box width and 38–42px tall. Align the shelf to the left edge of the writing box. Give the shelf its own clearly visible mid-graphite tone and restrained 8px corners. It must be a separate section below the text box, not the bottom band of one large card.

The tooling shelf contains only prompt-scoped controls: attachment icon, Claude Sonnet provider/model, High effort, Auto permission shield, and 3 files/context. Use lightweight icon-plus-short-value groups with whitespace; no separator lines, no pills, no boxes. Remove the git branch/worktree control entirely from the composer shelf. Keep the violet 42px send key as a separate button aligned to the far right beneath the writing box, on the same baseline as the shelf, with empty breathing space between shelf and send.

Relocate the session git context into the center conversation header beside “Visual design pass”: a compact branch icon plus “chore/visual-design-pass” in muted text. Remove the duplicate branch label from the far-right global title bar so the branch appears once. Do not add a border or pill around it. Preserve all other icons and UI exactly. Production IDE screenshot, no additional changes.
