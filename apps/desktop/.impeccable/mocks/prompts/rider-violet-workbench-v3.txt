Correct this exact cw-code Rider Violet Workbench screenshot while preserving the full three-region structure, pane proportions, session list, thread content, right file/diff/terminal workspace, icon toolbar, dark Rider palette, and code details.

The center thread must match the work-log behavior of the supplied T3 Code reference: remove the words “You”, “Claude”, “Assistant”, and every other persistent speaker label completely. Also keep all avatars/icons and resting timestamps absent. Do not replace them with bubbles or colored identity strips. The user request should appear as a simple compact text block aligned naturally in the reading column; the agent response and task results flow underneath as a continuous coding record. Timestamp and copy controls are hover-only and must not appear in this resting screenshot.

Rebuild the composer to correct the over-minimal result. It should be a clear floating work surface separated from the near-black conversation canvas by tonal elevation and a soft broad shadow, not by divider lines or a bright outline. Increase the total composer height to about 145–155px. The writing surface is 92–102px tall with 14px internal padding and a clearly lighter graphite/plum tone than the thread. Beneath it, create a 40–44px tooling shelf in a second, subtly darker-but-still-visible graphite tone. The tooling shelf must be unmistakable against both the writing plane and page background. Keep provider, model, effort, permissions, context/files, and branch controls readable as small icon-plus-short-value groups. Separate them with 12–18px whitespace only: absolutely no vertical separators, horizontal rule, segmented boxes, or individual pill backgrounds. Keep the 40–42px violet send key at the far right with a small breathing gap.

Use one restrained 8–10px outer corner radius for the composer silhouette, not an oversized pill. Keep the darker, low-accent conversation canvas. No new UI, no speaker labels, no decorative cards, no marketing elements.
