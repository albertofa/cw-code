Use case: ui-mockup
Asset type: high-fidelity 1920×1080 desktop application screenshot comp, “T3 Workbench”
Primary request: Design a third composition of cw-code using the SAME EXISTING shell: project-grouped sessions on the left, conversation in the center, contextual tools on the right. This image shows the tooling-expanded state for a developer actively reviewing changes. It must look like a close, believable sibling of the supplied T3 Code UI, colored and clarified with Rider, and using VS Code only for dense docked tooling behavior. No new navigation metaphor.
Input images: Image 1 is the primary structural and product-hierarchy reference (T3 Code); Image 2 contributes graphite surface hierarchy, restrained selection, and crisp type (Rider); Image 3 contributes the behavior and density of an expanded editor/terminal workspace (VS Code). Do not copy their product names/content or add a VS Code activity bar.
Composition/framing: full-bleed native Windows app, 1920×1080. Compact 40px titlebar. Left sidebar about 296px. Center conversation about 50% of the remaining work area. Right contextual workspace expanded to roughly 560px and split vertically inside itself: top two-thirds a Files/Diff editor, bottom one-third a real terminal. The split has a quiet drag handle, not a strong rule. These are docked views inside the existing right region, not an additional fourth column or bottom pane across the whole app.
Left sidebar: “cw-code”, Search, All projects, project groups with multiple sessions and restrained Running/Input/idle signals. Active “Visual design pass” is a soft graphite selected row. Keep metadata quiet and the project grouping very close to T3 Code.
Center: single-line header “cw-code / Visual design pass”. Real work transcript on an open canvas with one short request, one compact assistant answer, and one small completed execution summary “3 actions”. Keep it readable despite the larger tool pane. Sticky bottom composer with “Ask for changes or start another task”, Claude Sonnet, Auto, attach, send, plus a narrow worktree/branch footer.
Right workspace: compact labelled tabs “Files  Diff  Agents  Terminal”, Diff active. File list and code diff/editor occupy the top area with syntax color and green/red change regions. Bottom terminal shows only 5–7 concise lines such as “pnpm typecheck” and “Done in 4.2s”. Tabs, editor, and terminal should feel like one configurable workbench region. Add minimal collapse/split controls.
Style/medium: production-ready native desktop developer tool screenshot. Matte near-black/charcoal, opaque surfaces, crisp Segoe UI Variable-like sans for UI, legible mono for code/terminal. Comfortable 112% scale while preserving IDE density: 14–15px UI, 13.5–14px code, 38–42px rows, 16–18px icons.
Color palette: #090a0c, #101216, #171a1f, selected #252a31, off-white #e6e8eb, muted #8d949f, restrained blue #4d8dff, green additions/success, red deletions/error, amber waiting, coral provider identity sparingly.
Text (verbatim): “cw-code”, “Search”, “All projects”, “Visual design pass”, “Realtime auth”, “Running”, “Input”, “Files”, “Diff”, “Agents”, “Terminal”, “3 actions”, “Ask for changes or start another task”, “Claude Sonnet”, “Auto”, “pnpm typecheck”, “Done in 4.2s”.
Constraints: this is the same T3-like shell in a tooling-heavy state. The center remains a conversation, the right remains contextual and collapsible/resizable. Use surface contrast, alignment, whitespace, and active fills before borders.
Avoid: dashboards, project boards, session ribbons, new activity rails, permanent bottom panel spanning the app, card grids, message bubbles, excessive pane dividers, outlined cards, many pills, gradients, glow, glass, giant headings, marketing design, verbose prose, fake stats, device frame, watermark.
