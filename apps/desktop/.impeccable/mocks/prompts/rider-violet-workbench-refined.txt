Create a high-fidelity 1920×1080 desktop application UI comp for cw-code, an Electron coding-agent workbench. EDIT the supplied Rider Violet Workbench reference rather than inventing a new shell. Preserve its exact three-region composition: project-grouped sessions on the left, active conversation in the fluid center, contextual tool workspace on the right with file tree + diff above terminal. Preserve the larger comfortable scale, stepped graphite/plum Rider-style surfaces, subtle violet title chrome, crisp typography, restrained cyan/violet/green/amber/coral accents, and the current realistic content.

Make this refinement visibly more mature and less verbose. Replace global and contextual action labels with familiar IDE icons and tooltips implied: plus/new task, folder/open, pull request, search, overflow, files, diff, agents, terminal. In the right workspace use a compact icon strip; only the active tool may show a short label. Keep text where it carries non-inferable information: project/session names, file names, branch, statuses, conversation, commands, and diff content. Remove nearly all borders and divider lines. Separate regions through tonal depth, narrow gutters, padding, alignment, and active fills. Avoid cards around ordinary rows.

Redesign the bottom composer so it is clearly NOT the T3 Code rounded input box. Build a unique cw-code Command Deck in two distinct planes: the writing canvas is a broad calm borderless graphite surface that visually rises from the thread, with placeholder “Ask cw-code — @ files, / commands, $ skills”. Beneath it is a separate narrow utility rail, slightly offset and shorter than the writing plane, holding compact controls: attachment icon, provider mark + “Claude Sonnet”, effort “High”, permission shield, context meter, and worktree/branch. A detached rounded-square violet send button sits outside the rail at the far right, aligned like a hardware key. No enclosing outline around the whole composer. Keep it professional, precise, Rider-like, and buildable.

The screen must look like a production desktop IDE, not a marketing image. No decorative gradients beyond subtle top-chrome tint, no glassmorphism, no excessive rounded cards, no extra navigation rail, no giant headings, no fake dashboard. Use Segoe UI Variable style sans for UI/prose and monospace only for code/paths/telemetry.
