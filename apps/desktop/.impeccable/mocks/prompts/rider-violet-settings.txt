Create a high-fidelity 1920×1080 desktop application UI comp for cw-code showing its Settings / Configuration workspace. Use the supplied Rider Violet Workbench image as the visual identity anchor: same graphite/plum Rider-style surfaces, subtle violet top chrome, comfortable 14–15px scale, crisp IDE density, cyan/violet/green/amber/coral accents, Segoe-like sans with monospace only for paths and versions. Do not copy the reference conversation content.

Keep the desktop title bar and preserve a faint sense of the normal three-region workbench behind the settings state, but let Settings occupy the main center and right work area as a focused workspace rather than a small generic modal. The leftmost app sidebar remains narrow and recognizable with projects/sessions de-emphasized. Add a compact settings navigation column with icons and short labels only: General, Appearance, Providers, Source Control, Worktrees, Permissions. Providers is active with a violet inset marker.

The main settings composition is a dense master-detail provider configuration inspired by good IDEs: a slim provider roster at left with Claude Code, OpenCode, and Codex icons, green availability dots, version metadata, and a plus icon at the bottom; a wide selected-provider detail at right for Claude Code. Show only real cw-code controls: Binary path with compact folder/recheck icons, Launch arguments, enabled model rows with star-default icon and toggles, one compact custom model row, and an understated health strip showing CLI available and version. Controls align in a clean right column. Bottom actions are icon-led close/reset plus one restrained violet “Save” action; avoid repeated Save buttons.

Use broad tonal wells and whitespace instead of card grids. Remove routine borders and horizontal separators; reserve an outline only for focused inputs. Avoid explanatory paragraph clutter: short labels, one-line helper text only where essential. No glassmorphism, no marketing illustration, no oversized heading, no excessive pills, no fake settings categories or unsupported commercial claims. The result should feel like Rider’s settings clarity mixed with ZCode’s operational organization, but distinctly cw-code.
