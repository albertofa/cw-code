Create a high-fidelity edit of this exact cw-code Rider Violet Workbench screenshot, changing only the composer at the bottom of the center conversation. Preserve every other element exactly: label-free work log, message alignment, session header Git context, sidebar, right tooling pane, Rider palette, type, and scale.

Composer concept: UTILITY SPINE. Build one cohesive continuous graphite composer shell about 760px wide and 148–156px tall, with one outer silhouette, one shadow, and restrained 10–12px rounding. Instead of a bottom shelf, form a compact 42–46px internal vertical spine along the left edge of the same shell using a slightly darker graphite tone that touches the shell edges seamlessly—no gap, border, or divider rule. Stack three familiar icon-only controls in the spine: add/attachment, @ file/context, and slash command/skill. These are tooltips-on-hover, no visible labels.

The writing surface occupies the remaining large area to the right, with 15px padding and placeholder “Ask cw-code — @ files, / commands, $ skills”. Along its lower-right baseline, place a quiet inline runtime cluster: provider mark + “Claude Sonnet”, “High”, shield + “Auto”, and “3 files”, separated by generous whitespace only. Integrate the 40px violet send key into the lower-right corner of the same outer shell. No footer band, no shelf, no individual pill backgrounds, no separator lines. Git is absent from the composer.

The result must read as one authored Rider command instrument: tools on the vertical axis, writing dominant, runtime configuration subordinate. It must be materially different from a conventional chat box, while remaining practical and buildable. No other UI changes.
