Create a high-fidelity 1920×1080 desktop application UI comp for cw-code showing a live multi-agent session. Use the supplied Rider Violet Workbench image as the visual identity anchor: same fixed left project/session sidebar, fluid center conversation, and contextual right workspace; same graphite/plum Rider-style surfaces, subtle violet top chrome, comfortable scale, crisp IDE density, minimal borders, and disciplined violet/cyan/green/amber/coral status accents.

Keep the center conversation visible and realistic, with one compact delegation/tool card showing three parallel tasks and short outcomes. In the right workspace activate the Agents tool using an icon-first header: icons for Files, Diff, Agents, Terminal, with only “Agents” text visible beside the active icon. Replace the old text-heavy stacked subagent cards with an operational split inspector. The left portion of the right pane is a compact run list/tree: colored identity dot, agent name, one-line task, status glyph, elapsed time, and tiny token count. Show one running agent, two completed, and one queued/background item. Identity colors are separate from status colors.

The larger selected-agent inspector shows the current step at top, then a lean append-only event ledger inspired by DeepSeek Harness: prompt, tool call, result/evidence, and final output rows with small type glyphs, timestamps, and sparse syntax-colored snippets. Include a narrow metrics strip with 1 active, elapsed time, tokens, and tools; include icon-only copy, locate-in-thread, and overflow actions. Show worktree/branch as compact metadata. Do not duplicate the whole chat or create a dashboard.

At the bottom of the center thread use the same distinctive cw-code Command Deck concept: borderless writing canvas above a separate slim utility rail, detached violet send/stop button outside the rail. Most global actions are familiar icons without labels. Use tonal depth and spacing for separation; nearly no divider lines, no card soup, no giant title, no extra left activity rail, no marketing illustration. The result should feel like Rider visual clarity with ZCode task organization and DeepSeek traceability, while staying structurally cw-code.
