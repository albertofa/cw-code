Refine this exact cw-code desktop UI screenshot without changing its three-region layout, Rider Violet palette, content, scale, or pane proportions. Remove the two visible tooltip popovers under the top action icons (“New task” and “Create PR”); the toolbar must show clean icon buttons only. Keep only the active right-workspace label “Files”; the other right tool choices should remain icons only. Make the composer’s violet send key visibly detached from the thin lower command rail by a small clear gap, while preserving the broad borderless writing plane above. Remove any unnecessary fine divider lines between ordinary conversation task rows; use tonal rhythm and spacing instead. Everything else remains pixel-faithful to the supplied screenshot. Production IDE screenshot, no new controls, no marketing treatment, no extra text.
